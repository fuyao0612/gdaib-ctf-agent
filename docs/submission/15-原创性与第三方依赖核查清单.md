# 原创性与第三方依赖核查清单

原创部分：任务快照、策略边界、证据/结果模型、恢复检查点、通用评测索引和工作台交互。

第三方依赖：FastAPI、Pydantic、LangGraph、React、SQLite、MCP、PyYAML、httpx 等，版本见 `requirements.lock` 与 `apps/web/package-lock.json`，遵循各自许可证。2026-08-28 曾发现 3 个高危传递依赖（均在前端构建/lint 链路）；已执行 `npm.cmd audit fix --package-lock-only --omit=dev`，更新 `brace-expansion`、`postcss`、`nanoid` 锁定版本，并重新运行 lint、typecheck、Vitest、生产构建和 `npm audit --omit=dev --audit-level=high`，当前报告 0 vulnerabilities。
