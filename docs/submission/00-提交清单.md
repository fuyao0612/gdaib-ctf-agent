# 提交清单

程序：源代码、`compose.yaml`、`Dockerfile.*`、`yuwang.ps1`、`scripts/`。

文档：本目录 01-17、`docs/testing.md`、`docs/deployment.md`、`docs/evaluation.md`；其中 10 为开发文档，17 为方案设计文档。可直接阅读的 DOCX/PDF/PPTX 成品位于 `output/submission/`。

声明：按组委会模板补交盖章报名表 PDF、原创性与保密声明函。提交截止正式方案要求为 2026-09-05；邮件主题和百度云盘截图按方案执行。

提交前运行：`.\yuwang.ps1 check`，并确认 `git status` 不含 `.env`、`data/`、日志和缓存。提交包另附正式盖章报名表及原创性/保密声明，不使用草案代替，也不将签字盖章件提交到公开 Git 仓库。
