# 测试与评测报告

基线（2026-08-28）：后端 `pytest` 全部通过，覆盖率 87%（门槛 85%）。真实 Provider 测试默认跳过，未生成伪造成绩。上传类型一致性新增单元/API 测试；黄金案例包通过 manifest/Judge 结构检查。

真实评测入口为默认工作台 `http://127.0.0.1:8080/`（2026-08-28，DeepSeek `deepseek-v4-flash`）；这不是独立黄金 Compose 卷，后者仍未配置 Provider，不能把本段结果称作独立卷验收。A 三次真实 Run 为 1 次失败、2 次 `8/8` 通过，pass@1=0%、前三次至少一次成功率=100%；B 三次均 `6/6` 通过，pass@1=100%、前三次至少一次成功率=100%。C 首三次均未全通过（分别 `4/9`、`4/9`、`6/9`）；修复题面双工具契约及真实工具证据绑定后，后续两次为 `8/9`、`9/9`，最终 `9/9` Run 用时 54.248 秒、6 次 Provider 请求、38,056 Token、2 次工具调用、0 次重规划、0 次人工介入。成本由 Provider 返回 `0.0`，不得据此推算或宣称免费。所有 Run 摘要、失败轨迹和脱敏审计索引存于私有评测备份，提交材料仅引用脱敏 Run ID 与确定性 Judge 结论。
