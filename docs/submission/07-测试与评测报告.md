# 测试与评测报告

基线（2026-08-28）：后端 `pytest` 全部通过，覆盖率 87%（门槛 85%）。真实 Provider 测试默认跳过，未生成伪造成绩。上传类型一致性新增单元/API 测试；黄金案例包通过 manifest/Judge 结构检查。

真实评测命令：`python -m yuwang.evaluation run --case <case> --provider-id <UUID> --attempts 3`。统计 API 会输出 pass@1/pass@3、耗时中位数、平均 Token/成本/工具调用/重规划/人工介入和失败分类；每条记录保留 Provider/模型请求、确定性验证状态和轨迹引用。真实 Provider 数据（2026-08-28，隔离 demo 卷）：A 最近一次 Run completed，5 次工具调用/9 次 Provider 请求，私有 Judge 通过但未调用最后的候选验证工具，评测 5/6；B 最近一次 Run completed，7 次工具调用/12 次 Provider 请求，外部证据与私有 Judge 均通过，评测 6/6；C 多次尝试中安全标记、拒绝事件和 Judge 补全已验证，但 Provider 有一次等待澄清/超时、一次停止，未形成完整通过记录。以上是事实记录，不是 pass@1/pass@3 样本；后续应按每案例 3 次执行，并以 `/api/v1/evaluations/statistics` 导出的持久化记录为准。
