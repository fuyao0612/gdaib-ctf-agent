# 原创性与第三方依赖核查清单

原创部分：任务快照、策略边界、证据/结果模型、恢复检查点、通用评测索引和工作台交互。

第三方依赖：FastAPI、Pydantic、LangGraph、React、SQLite、MCP、PyYAML、httpx 等，版本见 `requirements.lock` 与 `apps/web/package-lock.json`，遵循各自许可证。2026-09-03 封版核查时发现并升级了仅位于前端开发链路的 `browserslist` 受影响版本；重新执行 `npm ci`、lint、typecheck、Vitest、生产构建、完整 `npm audit` 和 `npm audit --omit=dev`，当前完整与生产审计均为 0 vulnerabilities。
