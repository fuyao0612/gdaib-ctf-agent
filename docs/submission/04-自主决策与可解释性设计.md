# 自主决策与可解释性设计

模型只输出结构化计划/动作，服务端记录公开的目标、行动理由、工具参数摘要、观察事实、下一步决策和证据引用，不保存隐藏思维链。每个动作都有策略判定和状态事件；无进展、预算、超时、越权和不可信输出会触发停止或重规划。

可复查证据：Run events、ExecutionStep、EvidenceRecord、TaskResult、Markdown/JSON 报告；恢复从持久化检查点继续，不重放已完成调用。
