# 变更记录

## v0.5.2（开发中）

- 默认安全 Agent 改为 dynamic + verified，首次任务默认展示任务理解、计划、验证与自动调整；低延迟 direct 模式仍可手动选择。
- 工具观察补充关键词命中、IOC 类型、时间线类别、JWT 风险和 PCAP 协议等确定性公开事实，并强化收尾动作的证据引用契约。
- 成功工具调用与独立结论验证分级呈现；新增 JWT 静态研判正式验收任务，覆盖完整结果、证据、授权和预算链路。
- 工具步骤改为展示由结构化输出生成的确定性观察，并由下一次真实公开动作回填决策。
- CTF 报告从持久化轨迹生成时间线、线索、候选 Flag、Artifact、复现步骤和验证边界；Markdown 与 JSON 使用同一份数据。
- 修复独立导入运行轨迹服务时的循环依赖，并兼容历史默认报告模板的双 H1 问题。
- 完成初审方案设计、开发、测试、用户手册、技术报告与方案介绍成品，并更新界面证据和依赖审计。
- 升级前端开发链路的 `browserslist` 锁定版本，完整和生产 npm 审计均为 0 vulnerabilities。

## v0.5.1

- 新增持久化 Action + Observation 执行步骤、统一运行指标和 `trajectory.json` 导出。
- 报告升级为 schema 2.0，失败报告复用真实持久化调用和步骤指标。
- Web 默认展示执行时间线，并支持本地只读导入回放轨迹。

## v0.5.0

- 新增独立普通聊天链：自然语言多轮对话、标准 SSE 流式输出、完整响应降级、停止和重试。
- 新对话默认使用“对话”；只有明确选择“Agent 任务”才创建 Run 并进入计划与验证流程。
- Web 改为浅色聊天优先布局，侧栏可折叠，运行审计改为按需抽屉，输入区保持可见。
- Agent 任务时间线支持 Task Brief、计划、暂停、继续、按序追加指引及重规划状态展示。
- 设置中心新增默认聊天模型、默认回复方式、系统提示词、流式、上下文和界面偏好。
- 完成长对话与 1024×768 至 2048×1152 六种窗口的滚动、遮挡和横向溢出验收。
- 保持历史 Thread、Run、Provider、AgentProfile 和 SQLite 数据兼容。

## v0.4.2

- 修复启动、诊断、Provider 配置和生产检查中的兼容性问题。
