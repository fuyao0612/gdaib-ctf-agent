# 验收任务

这里的资料与开发回归输入隔离。验收应同时检查具体结构化结果、成功工具调用、Evidence 来源、授权范围、预算，
以及在失败后产生的公开重规划事件；模型的自述文本不构成通过依据。

验收覆盖复杂 IOC、多层编码、多 Artifact 关联、失败后重规划、localhost Web 和 JWT 静态研判；
全部任务只使用受控本地输入，不执行载荷或连接公网。

localhost Web 验收固定读取正式入口的 `/api/v1/health`（授权主机仍为 `127.0.0.1:8080`），不使用不存在的兼容路由，也不自动跟随页面链接。
